using UnityEngine;

namespace _Game._Scripts
{
    public class EnemyShieldL : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
